/// Canonical sizing tokens for fixed-dimension UI elements.
///
/// Use these instead of hard-coded numeric values for rings, icon tiles, etc.
abstract final class AppSizing {
  /// Circular progress / strength ring diameter on profile strength card.
  static const double progressRing = 44;

  /// Icon tile size used in help/safety and similar list rows.
  static const double iconTile = 44;
}
