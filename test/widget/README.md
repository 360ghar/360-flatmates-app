# Flutter Widget Tests

Use this folder for isolated widgets and page states that can run with `flutter test`.

Prefer widget tests for loading, empty, error, validation, localization, and accessibility semantics. Use Maestro only when device shell behavior is required.
