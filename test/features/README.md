# Flutter Feature Tests

Use this folder for feature-level tests organized by domain, for example `test/features/chats/` or `test/features/discover/`.

Feature tests should compose providers/controllers and verify complete user behavior without requiring a physical device. Add story IDs from `FEATURE_AUDIT.xlsx` to test names.
