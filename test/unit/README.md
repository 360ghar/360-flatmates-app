# Flutter Unit Tests

Use this folder for pure Dart behavior: DTO parsing, repositories with fake clients, validators, mappers, interceptors, and controller logic that does not need widget rendering.

Include the canonical `FL-FEATURE-###` story ID in each test name when the unit test covers a story behavior.
